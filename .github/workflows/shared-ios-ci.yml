name: Shared iOS CI

on:
  push:
    branches: [ main ]
    paths:
      - 'shared/**'
      - 'iosApp/**'
      - 'build.gradle.kts'
      - 'settings.gradle.kts'
      - 'gradle.properties'
      - 'gradlew'
      - 'gradle/**'
      - '.github/workflows/shared-ios-ci.yml'
  pull_request:
    branches: [ main ]
    paths:
      - 'shared/**'
      - 'iosApp/**'
      - 'build.gradle.kts'
      - 'settings.gradle.kts'
      - 'gradle.properties'
      - 'gradlew'
      - 'gradle/**'
      - '.github/workflows/shared-ios-ci.yml'
  workflow_dispatch:

concurrency:
  group: shared-ios-ci-${{ github.ref }}
  cancel-in-progress: true

permissions:
  contents: read

jobs:
  arm64:
    name: Build & test shared iOS (arm64)
    runs-on: macos-15
    timeout-minutes: 30
    steps:
      - name: Checkout
        uses: actions/checkout@v7
        with:
          persist-credentials: false

      - name: Set up JDK 17
        uses: actions/setup-java@v6
        with:
          distribution: temurin
          java-version: '17'

      - name: Set up Gradle
        uses: gradle/actions/setup-gradle@v6.3.0

      - name: Build XCFramework and run iOS tests
        run: >-
          ./gradlew --no-daemon --stacktrace
          :shared:assembleAutkaSharedDebugXCFramework
          :shared:iosSimulatorArm64Test

      - name: Build native iOS app
        run: >-
          xcodebuild
          -project iosApp/iosApp.xcodeproj
          -scheme iosApp
          -configuration Debug
          -sdk iphonesimulator
          -destination 'generic/platform=iOS Simulator'
          CODE_SIGNING_ALLOWED=NO
          build

      - name: Upload debug XCFramework
        uses: actions/upload-artifact@v7
        with:
          name: AutkaShared-debug-xcframework
          path: shared/build/XCFrameworks/debug
          if-no-files-found: error

  x64:
    name: Build & test shared iOS (x64)
    runs-on: macos-15-intel
    timeout-minutes: 30
    steps:
      - name: Checkout
        uses: actions/checkout@v7
        with:
          persist-credentials: false

      - name: Set up JDK 17
        uses: actions/setup-java@v6
        with:
          distribution: temurin
          java-version: '17'

      - name: Set up Gradle
        uses: gradle/actions/setup-gradle@v6.3.0

      - name: Run iOS tests
        run: ./gradlew --no-daemon --stacktrace :shared:iosX64Test
